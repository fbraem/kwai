{
  "name": "kwai",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel --continue",
    "build": "turbo run build"
  },
  "dependencies": {
  },
  "devDependencies": {
    "eslint-config-kwai": "*",
    "typescript": "^5.1.3",
    "turbo": "latest",
    "vite": "^4.3.9"
  }
}
